import { FieldLengthDirective } from './field-length.directive';

describe('FieldLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldLengthDirective();
    expect(directive).toBeTruthy();
  });
});
