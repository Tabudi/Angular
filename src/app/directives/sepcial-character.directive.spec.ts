import { SepcialCharacterDirective } from './sepcial-character.directive';

describe('SepcialCharacterDirective', () => {
 /* it('should create an instance', () => {
    const directive = new SepcialCharacterDirective();
    expect(directive).toBeTruthy();
  });*/
});
